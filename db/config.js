const config = {
        host: "host address**",
        user: "user-name**",
        password: "db password**",
        database: "db name**",
        port: 'port number**',
        SECRET: "secret key**"
    }

module.exports = config;